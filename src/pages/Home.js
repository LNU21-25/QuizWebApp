export default function renderHome(container) {
    container.innerHTML = `
      <section>
        <h1>Welcome to the Quiz Game</h1>
        <p>This is the home page. Select a quiz or view the leaderboard.</p>
      </section>
    `;
  }